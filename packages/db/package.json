{
  "name": "@benkyou/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./schema": {
      "default": "./src/schema.ts"
    },
    "./drizzle": {
      "default": "./src/drizzle.ts"
    }
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:pull": "drizzle-kit pull",
    "db:studio": "drizzle-kit studio",
    "db:verify": "bun run verify-tables.ts",
    "db:create-tables": "bun run create-tables.ts",
    "db:migrate-to-notes": "bun run migrate-to-notes.ts",
    "db:migrate-to-study": "bun run migrate-to-study-system.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.47.10",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.0",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.1",
    "@types/pg": "^8.11.10",
    "@benkyou/config": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}

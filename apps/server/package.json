{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "cd ../.. && bun --hot apps/server/src/index.ts",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@benkyou/auth": "workspace:*",
    "@benkyou/db": "workspace:*",
    "@benkyou/fsrs": "workspace:*",
    "@elysiajs/cors": "^1.3.3",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.0",
    "elysia": "^1.3.21",
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/bun": "^1.2.6",
    "tsdown": "^0.16.5",
    "@benkyou/config": "workspace:*"
  }
}
